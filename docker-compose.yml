version: '3'

services:
  fastapi-app:
    restart: always
    build:
      context: .
      dockerfile: nonroot.Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - .env
  nginx:
    image: nginx:latest
    volumes:
      - ./assets/nginx.http.config:/etc/nginx/nginx.conf
      # - ./assets/nginx.https.config:/etc/nginx/nginx.conf
      # - ./assets/certs:/etc/nginx/certs
    ports:
      - "80:80"
      - "443:443"

#volumes:
#  minio_data:
#      driver: local